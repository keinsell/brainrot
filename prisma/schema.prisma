generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["tracing", "metrics", "fieldReference"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URI")
}

model Account {
  id       String @id @default(cuid())
  username String @unique
  password String
}
