version: "1.1"
name: Account Registration
env:
  protocol: http
  host: localhost
  port: 45483
tests:
  example:
    steps:
      - name: GET request
        http:
          url: https://example.com
          method: GET
          check:
            status: /^20/
  register-account:
    steps:
      - name: POST /account/
        http:
          url: ${{env.protocol}}://${{env.host}}:${{env.port}}/account
          method: POST
          check:
            status: /^20/
          headers:
            Content-Type: application/json
          json:
            username: ${{ internet.username | fake }}
            password: fasasfaasdasdasd
            email: keinsell@protonmail.com