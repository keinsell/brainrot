/**
 * Represents an access token.
 *
 * @typedef {string} AccessToken
 * @description A string that represents an access token, which is used for authentication and authorization purposes.
 */
export type AccessToken = string;